A pyinstaller discovery trouble (?) was encountered, which manifests 
during app execution, during import of detectron2, before the wx UI is 
displayed, like

    Traceback (most recent call last):
      File "torch/_sources.py", line 23, in get_source_lines_and_file
      File "inspect.py", line 1121, in getsourcelines
      File "inspect.py", line 958, in findsource
    OSError: could not get source code

    The above exception was the direct cause of the following exception:

    Traceback (most recent call last):
    ...
    OSError: Can't get source for <class
    'LabGym.detectron2.modeling.box_regression.Box2BoxTransform'>.
    TorchScript requires source access in order to carry out compilation,
    make sure original .py files are available.
    [PYI-85218:ERROR] Failed to execute script 'myapp' due to unhandled
    exception!


[W1]
One workaround to avoid fatal error during startup, is to compile with
myapp.py containing 
    os.environ['PYTORCH_JIT'] = '0'
because (from https://docs.pytorch.org/docs/stable/jit.html),
    Setting the environment variable PYTORCH_JIT=0 will disable all
    script and tracing annotations. If there is hard-to-debug error in
    one of your TorchScript models, you can use this flag to force
    everything to run using native Python. Since TorchScript (scripting
    and tracing) is disabled with this flag, you can use tools like pdb
    to debug the model code.

John is uncertain about the performance impact of this workaround.
The better solution would be to identify the libraries/files that
are required but not automatically discovered, and specify them to
pyinstaller for inclusion.


[W2] 
Instead of [W1], these sets of options were tried, but they do not 
change the fatal error during initialization.
(a)
    --collect-all torch 
    --collect-all torchvision
(b)
    --collect-all torch 
    --collect-all torchvision
    --collect-all detectron2


[W3]
Instead of [W1], this works (*)... supply pyinstaller options
(a)
    --collect-all torch 
    --collect-all torchvision
    --collect-all detectron2
    --collect-all LabGym.detectron2
(b) this also works (*)
    --collect-all LabGym.detectron2
* that is, the resultant executable proceeds to display the wx UI.


[W4]
google search: pyinstaller hook for torch
yields
    PyInstaller requires specific handling for complex packages like
    torch due to their dynamic imports, C extensions, and data files.
    While PyInstaller includes a torch hook in its
    pyinstaller-hooks-contrib package, manual adjustments or
    troubleshooting may still be necessary.
    ...
    *   Troubleshooting Missing Modules/Files: 
        If the executable fails to run or reports missing modules, you 
        may need to explicitly include them.  

        Hidden Imports: Use --hidden-import to ensure PyInstaller
        includes modules that are dynamically imported by torch or its
        dependencies (e.g., torchvision, numpy, etc.).
            --hidden-import=torch --hidden-import=torchvision 

        Collecting All: For packages with numerous submodules or data 
        files, --collect-all can be used to ensure all necessary 
        components are included.
            --collect-all torch --collect-all torchvision
    
        Edit the your_script.spec file to include hiddenimports, datas,
        binaries, and potentially module_collection_mode settings (e.g.,
        for torchvision to collect source .py files).  

    Specific Considerations for Torch: 
      CUDA/GPU Dependencies: 
        If your torch installation uses CUDA, ensure the necessary CUDA 
        runtime libraries are available to the bundled application. 
        Sometimes, installing torch via pip (instead of conda) can 
        simplify this.  
      JIT and Source Files:
        Warnings related to JIT compilation or missing source .py files
        (e.g., from torchvision) can often be resolved by setting
        module_collection_mode in the .spec file to ensure source files are
        collected.

        module_collection_mode = {
            'torchvision': 'pyz+py',
        }
    
    Dynamic Libraries (DLLs/DYLIBs): Ensure that all required dynamic
    libraries (e.g., .dll on Windows, .dylib on macOS) are correctly
    bundled, especially those related to torch's C extensions or
    dependencies like torchtext. You might need to manually add them via
    binaries in the .spec file if PyInstaller misses them.
