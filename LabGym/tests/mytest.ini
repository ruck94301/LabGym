# Forces you to annotate every function signature.
disallow_untyped_defs = true

# Crucial: Tells mypy to look inside functions even if the function
# itself isn't fully typed.
check_untyped_defs = true

# Prevents you from forgetting the return type (e.g., -> None).
disallow_incomplete_defs = true
